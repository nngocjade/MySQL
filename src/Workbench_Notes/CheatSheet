


SQL Language


CREATE SCHEMA `   `;

----------------------------------------------------------------------------------------------------

INSERT INTO

INSERT INTO `schema_name`.`table_name`(`column_1`,`column_2`,`column_3`, etc...)
VALUES (`column_1_value`, `   `, `   `, `    `, etc..);

examples:

INSERT INTO `mybatis-test`.`users` (`users_id`, `first_name`, `last_name`, `isActive`)
VALUES ('4', 'Sophia', 'Nguyen', '0');

----------------------------------------------------------------------------------------------------

UPDATE

UPDATE table_name
SET column1 = value1, column2 = value2, ...
WHERE condition;

examples:

UPDATE `mybatis-test`.users
SET first_name = #{first_name}, last_name = #{last_name}, isActive = #{isActive}
where id = #{id}";

UPDATE `mybatis-test`.task
SET user_id = 4
WHERE task_id = 2;


-----------------------------------------------------------------------------------------------------

DELETE (setting isactive to zero)

examples:

update `mybatis-test`.users
SET isActive = 0
where user_id = #{user_id};


DELETE (whole row)

DELETE FROM table_name
WHERE condition;

----------------------------------------------------------------------------------------------------







